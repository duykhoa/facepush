# /bin/bash

PWD = echo pwd

if [ ! -f ~/.tmuxinator/facepush.yml ]
then
  ln -s $PWD/facepush.yml ~/.tmuxinator/facepush.yml
fi

if [ ! -f ../.git/hooks/pre-push ]
then
  ln -s $PWD/pre-push ../.git/hooks/pre-push
fi

